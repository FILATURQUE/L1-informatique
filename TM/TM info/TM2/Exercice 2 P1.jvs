void main() {
    int n = readInteger("Chosir un nombre aleatoire");
    for (int i = 0; i < n + 1; i++) {
        println(i);
    }
}

// Si l'utilisateur entre un nombre negatif la boucle for ne s'execute pas donc le code ne
// s'execute pas
